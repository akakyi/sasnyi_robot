bot.token=${BOT_TOKEN}
bot.name=fm_noti_bot

spring.datasource.url=${DATABASE_URL:jdbc:postgresql://localhost:5432/kotlin_notify_db}
spring.datasource.username=${DATABASE_USERNAME:postgres}
spring.datasource.password=${DATABASE_PASSWORD:postgres}

amqp.channel.key=telega_new_channel

amqp.notify.key=kotlin_notify
